spring:
  application:
    name: LucyStar
  datasource:
    url: jdbc:postgresql://localhost:5432/lucystarDb
    username: postgres
    password: m256039l
    driver-class-name: org.postgresql.Driver  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 9001	

