spring:
  application:
    name: LucyStar
  datasource:
    url: jdbc:postgresql://localhost:5433/grtp2
    username: postgres
    password: m256039l
    driver-class-name: org.postgresql.Driver  
  jpa:
    properties:
      hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      show-sql: true
server:
  port: 9001	

  