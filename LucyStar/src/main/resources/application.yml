spring:
  application:
    name: LucyStar
  datasource:
    url: jdbc:postgresql://${DATABASE_HOST:127.0.0.1}:${DATABASE_PORT:5432}/lucystarDb
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASSWD:m256039l}
    driver-class-name: org.postgresql.Driver  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 9001	

